# Break-It Inventory — Live Contact & Click Surfaces

**Ticket:** #21 (part of #20 map) | **Live URL:** https://ericmanabat-dev.vercel.app/  
**Stack:** React 19 + Vite SPA, Tailwind v4, Vercel frontend, FastAPI Cloud Hobby sidecar (Gemini)  
**Branch:** `research/break-it-inventory` | **Date:** 2026-08-31 | **Scope:** AFK read-only inventory — facts + file:line pointers, no behavior decisions

> Generated by reading local files: `index.html`, `src/components/Contact.tsx`, `src/components/DigitalEricChat.tsx`, `src/data/caseStudies.ts`, `src/components/Projects.tsx`, `src/components/CaseStudyModal.tsx`, `src/components/Header.tsx`, `src/components/CustomCursor.tsx`, `src/components/Hero.tsx`, `src/components/ResumeModal.tsx`, `src/components/Credentials.tsx`, `src/hooks/useTheme.ts`, `src/App.tsx`, `public/`, `src/assets/`. No UAT — this unblocks the break-it matrix.

---

## 1. Contact Surface Truth

| Surface | Location (file:line) | Current guard / handler | What "break it" would attempt |
|---|---|---|---|
| **Email CTA — mailto only (no `<form>`)** | `src/components/Contact.tsx:23-29` `href="mailto:eric.manabatseam@gmail.com"` | Plain `<a>` with `mailto:`; no form, no validation, no JS handler. Relies on OS mail client. | Click with no mail client configured; mailto injection (`?subject=` with 10k chars / newlines); test that no `<form>` exists site-wide (grep confirms only chat has `<form>`) |
| **No site-wide `<form>`** | Verified: `Contact.tsx` has none; only `<form>` is `src/components/DigitalEricChat.tsx:199` | — | Submit empty/garbage if a form ever appears; confirm `grep -r "<form"` returns exactly 1 hit (chat) |
| **Digital Eric chat — text input** | `src/components/DigitalEricChat.tsx:206-213` `maxLength={500}`, `placeholder="Ask Digital Eric…"`, `aria-label="Message Digital Eric"` | `maxLength={500}` HTML attr; `onChange` → `setInput(e.target.value)` at `211`; `value={input}` controlled | Paste 600+ chars (browser truncates at 500 — test JS bypass via devtools `input.value = 'x'.repeat(2000)` + event); paste emoji/supplementary planes (count vs bytes); paste `<script>`, SQL, Taglish, newlines, zero-width chars |
| **Send — disabled gate** | `DigitalEricChat.tsx:214-221` `disabled={!input.trim() || streaming}` | Blocks empty / whitespace-only (`trim()`) and concurrent sends (`streaming` boolean) | Rapid double-click Send; Enter-spam while streaming; send whitespace `"   "`; send exactly `""` after trim |
| **Send — streaming double-submit gate** | `DigitalEricChat.tsx:21` `const [streaming, setStreaming] = useState(false)` → `40: if (!question || streaming) return` → `62: setStreaming(true)` → `102: setStreaming(false)` in `finally` | Early return if `streaming===true`; input cleared at `41` before fetch; flag reset even on throw | Click Send → immediately click starter chip (`STARTERS.map` at `161-169`) while `streaming`; hit Enter in input during stream; verify button stays disabled |
| **Starter chips bypass input** | `DigitalEricChat.tsx:4-9` `STARTERS` (4 prompts) rendered `161-169` each `onClick={() => send(s)}` | Chips call `send()` directly, not via input; still hit same `streaming` gate at `40` | Click 3 chips fast; click chip while streaming; click chip when `apiBase()` empty |
| **Chat — missing backend guard** | `DigitalEricChat.tsx:11-13` `apiBase()` reads `VITE_CHAT_API_URL`, `43-54` fallback when empty or `!apiBase()` | If empty, pushes user msg + assistant fallback `"My backend isn't wired up yet…"` and returns — no fetch | Unset `VITE_CHAT_API_URL` build; set it to `https://invalid.invalid`; set to trailing-slash edge (`///`); verify fallback renders without throw |
| **Chat — history window** | `DigitalEricChat.tsx:56` `messages.slice(-8)` sent as `history` | Only last 8 msgs sent; no server-side history pruning visible client-side | Fill 20-turn conversation, verify request body contains only 8; test token growth still respects `maxLength` per turn |
| **Chat — SSE streaming handler** | `DigitalEricChat.tsx:66-93` `fetch(.../chat)` POST JSON `{message, history}`, then `resp.body.getReader()` + `TextDecoder` + `buffer.split('\n\n')` + `data:` JSON parse `payload.token` | Checks `!resp.ok \|\| !resp.body` throws at `71`; loops `reader.read()`; `setLastAssistant` appends tokens; `[DONE]` ignored at `87`; empty reply fallback at `94`; cold start indicator `63,89,103` | Kill network mid-stream; backend returns non-SSE `200`; send SSE with `data:` missing / bad JSON; return `204` with no body; return `500`; return token `[DONE]` as first payload; disconnect reader early |
| **Chat — error surface** | `DigitalEricChat.tsx:97-100` `catch { setLastAssistant('I hit a snag…') }` | Generic catch, no retry, no error code exposed | Force `HTTP 429 / 500 / CORS` from `VITE_CHAT_API_URL`; offline mode; verify message replaces last assistant bubble (not appended) |
| **Chat — scroll & coldStart** | `DigitalEricChat.tsx:33-36` `scrollRef` → `scrollTo({top: scrollHeight})` on `[messages, coldStart]`; `174-181` coldStart pulse | Passive scroll helper; no guard for missing ref | Overflow 50 messages, verify auto-scroll doesn't fight manual scroll; assert `data-testid="cold-start"` appears then disappears |
| **Chat — open/close toggles** | `DigitalEricChat.tsx:109-125` FAB `aria-label="Chat with Digital Eric…"`, `143-149` close X | No `localStorage` persistence; open state is in-memory `useState(false)` | Refresh mid-chat (messages lost — expected?); Escape should NOT close (no handler — only X); test focus trap absent |
| **Resume — gated modal vs link** | `src/components/Contact.tsx:31-43` `<a href="/resume.pdf" target="_blank" rel="noopener noreferrer" onClick={(e)=>{e.preventDefault(); setIsResumeOpen(true)}}>` | `preventDefault` + state opens `ResumeModal`; fallback `<a>` href still valid if JS disabled (progressive enhancement broken intentionally by preventDefault) | Disable JS — click should navigate to `/resume.pdf`; with JS, assert no navigation occurs, modal opens; test middle-click / ctrl-click (preventDefault still fires — check) |
| **ResumeModal — PDF surface** | `src/components/ResumeModal.tsx:62-83` links `href="/resume.pdf"` + `100: <iframe src="/resume.pdf#toolbar=0">` | Two text links (Open + Download) plus iframe; `download="Eric_Manabat_Resume.pdf"` at `76`; modal `role="dialog" aria-modal` at `43` | Request `/resume.pdf` 404 (rename file); test iframe CSP / `X-Frame-Options`; mobile fallback bar `107` visible only `sm:hidden`; download attribute ignored cross-origin |
| **Body-scroll lock (ResumeModal)** | `ResumeModal.tsx:12-19` `document.body.style.overflow = hidden / ''` | No saved prior value (unlike CaseStudyModal); cleanup resets to `''` | Open ResumeModal → open CaseStudyModal → close one → verify scroll not stuck `hidden` |
| **Profile images** | `src/assets/profile.png` (bundled via `src/components/Hero.tsx:1` `import heroImg`) vs `public/profile.png` + `public/profile.webp` (static) | `Hero.tsx:102-106` `<img src={heroImg} alt="Eric…">` with `border-2` + `shadow-xl`; `index.html:5` `href="/profile.png"` favicon | Break image import (404); test `public/profile.webp` never referenced — orphan?; favicon missing still renders; slow image — layout shift (no `width/height`) |

---

## 2. Click-Everything — Links & Assets

### 2a. Case study external links (source of truth: `src/data/caseStudies.ts:66`)

| # | Case study | demo | github | sidecarGithub |
|---|---|---|---|---|
| 1 | `ceit-library` `PLV eLib` (`ceit-library:60`) | `https://ceit-library-main-cru0ty.laravel.cloud/` `:67` | `https://github.com/PSergio984/CEIT-Library` `:68` | `https://github.com/PSergio984/ceit-ai-sidecar` `:69` |
| 2 | `agos` (`agos:200`) | `https://agos-app.vercel.app/` `:208` | `https://github.com/PSergio984/agos-iot-flood-monitoring` `:209` | — |
| 3 | `task-buddy` (`task-buddy:320`) | `https://task-buddy-frontend.vercel.app/` `:329` | `https://github.com/PSergio984/task-buddy-frontend` `:330` | — |
| 4 | `survey-system` (`survey-system:391`) | `https://valenzuela-satisfaction-survey-main-plae88.laravel.cloud/` `:400` | `https://github.com/PSergio984/valenzuela-satisfaction-survey` `:401` | — |

Rendered at `src/components/Projects.tsx:148-184` (card footer) and `src/components/CaseStudyModal.tsx:143-177` (modal sidebar). All external links use `target="_blank" rel="noopener noreferrer"` (verified at `Projects.tsx:151-153, 162-165, 174-177` and `CaseStudyModal.tsx:146-149, 156-159, 167-170`).

**Break-it attempts:** click each with network throttled (4G/offline); verify `rel` present (no `window.opener` leak); confirm `sidecarGithub` only renders for `ceit-library` (`Projects.tsx:173` conditional); test demo 500 / GitHub repo made private / Vercel cold start.

### 2b. Navigation & chrome links

| Surface | Location | Href / target / rel | Break-it |
|---|---|---|---|
| Brand `ERIC.AI` | `Header.tsx:30-35` | `href="#about"` — no target/rel (same-page) | Hash spam `#about`, verify no reload |
| Header nav (6 items) | `Header.tsx:5-12` ids `[about,projects,experience,skills,credentials,contact]` → `Header.tsx:40-48` `href="#${id}"` | `aria-current="page"` when active via `useActiveSection` | Click each → assert `window.location.hash` + `activeSection` highlight (`Header.tsx:21-25` violet bg); rapid nav clicks; back-button after hash |
| Hero CTA — Schedule Interview | `Hero.tsx:153-159` | `href="#contact"` | Same as header; ensure smooth scroll not broken by `overflow-hidden` |
| Hero CTA — Explore Case Studies | `Hero.tsx:160-166` | `href="#projects"` | Same |
| Hero platform row (6 icons) | `Hero.tsx:8-45` `[github,linkedin,bootdev,tryhackme,hackthebox,hackerrank]` rendered `171-184` | All `target="_blank" rel="noopener noreferrer"`; hrefs: `https://github.com/PSergio984`, `https://www.linkedin.com/in/eric-gabriel-manabat`, `https://www.boot.dev/u/eric984`, `https://tryhackme.com/p/eric.manabatseam`, `https://ctf.hackthebox.com/user/profile/1014955`, `https://www.hackerrank.com/profile/eric_manabatseam` | Click each; assert `aria-label`/`title`; verify TryHackMe & HTB/HackerRank profile URLs 404 if username changes |
| Contact row (4 links) | `Contact.tsx:46,56,66` | `github/bootdev/linkedin` same hrefs as Hero, `target="_blank" rel="noopener noreferrer"` | Same; verify LinkedIn vs `www.linkedin.com/in/eric-gabriel-manabat` canonical; Boot.dev slug `eric984` |
| Footer | `Footer.tsx:5` | No links — only `© {getFullYear()}` | Year rolls on Jan 1 — test frozen clock |
| Credentials — proof images | `src/data/credentials.ts:14-274` 23 records with `imageUrl: /assets/certificates/*.webp` etc. | Rendered `Credentials.tsx:59-71` via `<img>` or `<iframe>` if `.pdf`; viewer opens on `210` button; `profileUrl` case `219-227` uses `<a target="_blank" rel>` | Break each `/assets/certificates/...` (case-sensitive); test PDF `Coursera 837103NQD2H1.pdf` iframe blocked by CSP; orphan assets with spaces in filename (`Coursera *.pdf`) need encode |
| Case-study card image button | `Projects.tsx:75-86` `<button onClick={() => openStudy}>` wrapping `<img src={project.imageUrl}>` | `project.imageUrl` values: `/assets/projects/ceit-lib.webp`, `/assets/projects/agos.webp`, `/assets/projects/taskbuddy.webp`, `/assets/projects/survey.webp` (`caseStudies.ts:65,206,326,399`) | 404 image still keeps button clickable; alt text `"{title} screenshot"`; test `object-cover` hides broken-image icon on some browsers |

### 2c. Static assets (`public/`)

`public/profile.png`, `public/profile.webp`, `public/resume.pdf`, `public/favicon.svg`, `public/icons.svg`, `public/assets/projects/*.{webp,png}`, `public/assets/certificates/*.{webp,pdf,jpg}`, `public/assets/cohorts/*.webp`, `public/assets/seminars/*.webp`, `public/assets/anthropic/*.webp` — verified via `ls public` + `public/assets` count (no `robots.txt`, `sitemap.xml`, `og-image` present).

---

## 3. Browser / Device-Sensitive Surfaces

| Surface | Location | Current guard / handler | What "break it" would attempt |
|---|---|---|---|
| **Custom cursor** | `src/components/CustomCursor.tsx:1-55` + `src/hooks/useMousePosition.ts:1-16` + `src/App.tsx:16` | Wrapper `hidden md:block` (`CustomCursor.tsx:40`) so invisible <768px; `pointer-events-none`; `mouseover` → `closest('a, button, input, [role="button"]')` expands ring; `mouseenter/leave` toggles `isVisible`; `mousemove` streams to `x,y` | Test on touch (iPad, Android) — hidden branch must not leave `null` ghost; disable `mousemove` via devtools; rapid `mouseover` on SVG `lucide-react` icons (`closest` on SVG target edge); `prefers-reduced-motion` not checked — assert ring animation still runs; SSR no `window` guard is implicit (hook uses `useEffect` only) |
| **Hash routing — case study deep link** | `src/components/Projects.tsx:12-46` | `handleHashChange` on mount + `hashchange` listener `:30`; alias `plv-elib ↔ ceit-library` at `:19`; `openStudy` → `pushState(null,'','#case-study/${id}')` at `:37`; `closeStudy` → `pushState` clearing hash at `:44`; `CaseStudyModal` renders when `selectedStudy !== null` | Direct nav to `/#case-study/agooo` (typo) → modal stays closed (correct?); `/#case-study/ceit-library` vs `/#case-study/plv-elib` alias; back button after `pushState`; hash with query `/#case-study/agos?x=1`; multiple `#case-study/` rapid switches; assert no `popstate` listener — back may not close modal |
| **Modal body-scroll lock** | `CaseStudyModal.tsx:39-52` caches `previousOverflow`, sets `hidden`, restores `previousOverflow \|\| 'auto'`; `ResumeModal.tsx:12-19` sets `''` (no cache); `Credentials.tsx:16,26` (`VerificationModal`) sets `hidden`→`auto` | Three modals use three different restore strategies — stacking can desync | Open CaseStudy → open ResumeModal → close ResumeModal → verify scroll not stuck `hidden`; open modal then hard refresh; test `document.body.style` override by extensions |
| **Modal dismiss handlers** | `CaseStudyModal.tsx:43-47` `Escape` → `onClose`, `68-69` backdrop `onClick={onClose}` + `76` `e.stopPropagation()`; `ResumeModal.tsx:24-27` same; `Credentials.tsx:18-21` same | No focus trap; `role="dialog" aria-modal="true"` present | Spam `Escape` with nested modals; click inside modal near edge (stopPropagation edge); `Tab` out of modal (focus leaks to page) |
| **Dark/light — FOUC script** | `index.html:14-22` blocking `<script>` before paint: `localStorage.getItem('theme')` + `matchMedia('(prefers-color-scheme: dark)')` → `classList.add('dark')` | Runs before React; prevents flash | `localStorage` blocked (private Safari) — `try/catch` absent in inline script (thin but throws?) vs `useTheme.ts:31-37` which does catch; `matchMedia` unsupported; toggle then hard reload → flicker on slow 3G |
| **Theme toggle system** | `src/hooks/useTheme.ts:1-88` `setTheme`/`toggleTheme` at `55,65` + `applyTheme` at `48` toggling `dark` class; `useTheme()` via `useSyncExternalStore`; listeners `matchMedia('change')` `23` + `storage` `24` | Supports `light/dark/system`; notifies subscribers; `Header.tsx:52-60` button `id="theme-toggle"` | Toggle 10× fast (race on `localStorage`); change OS theme while page open (should follow when `system`); cross-tab `localStorage` event; assert `document.documentElement.classList` matches |
| **Responsive layout** | Global: `src/index.css:65-73` `overflow-x:hidden` + `max-width:100vw`; `src/App.tsx:19` `max-w-6xl px-4 sm:px-6`; Header `Header.tsx:37` `overflow-x-auto scrollbar-none` nav; Projects grid `Projects.tsx:67` `grid-cols-1 md:grid-cols-2`; Modal left `CaseStudyModal.tsx:91` `max-h-[35vh] md:max-h-none` + right `overflow-y-auto`; Chat widget `DigitalEricChat.tsx:110` `bottom-5 right-5` closed FAB vs `128` `bottom-4 right-4 left-4 sm:left-auto sm:w-[380px] max-h-[70vh]` | Breakpoints: `sm 640`, `md 768`, `lg 1024`; mobile tab bar `md:hidden` at `CaseStudyModal.tsx:183` | Test 320px (iPhone SE) — header nav `overflow-x-auto` scroll hint invisible; `max-w-6xl` + `px-4` still causes `100vw` scrollbar if `overflow-x:hidden` removed; chat `left-4 right-4` on 280px screen; modal `35vh` leaves 65vh for right col — verify no double scrollbars |
| **Typography resp. width** | `Hero.tsx:94` `text-3xl sm:text-5xl md:text-6xl` headline + `Hero.tsx:100` image `w-24 h-24 sm:w-32 sm:h-32` | `break-words` on `h1` | Long name / narrow viewport — wrap vs truncate; `sm:text-5xl` at 639px still `text-3xl` — visual jump |
| **Fonts & preconnect** | `index.html:25-30` `preconnect fonts.googleapis.com/.gstatic` + Google Fonts `Outfit/JetBrains Mono/Space Grotesk` | No `font-display` swap (uses defaults) | Block `fonts.googleapis.com` (offline) — fallback fonts `system-ui` metrics; slow font — CLS on heading |
| **Body background fixed** | `src/index.css:79-87` `background-attachment:fixed` radial gradients | Fixed attachment is expensive on iOS Safari; may be disabled by browser | Verify on iOS — background scroll jank; `prefers-reduced-motion` not applied |
| **Active section observer** | `src/hooks/useActiveSection.ts:1-46` scroll+resize listeners, viewport-center heuristic | `passive:true` on scroll | Scroll fast between sections — active flickers; section `getElementById` returns null if id typo |

---

## 4. Gaps Explicitly Called Out by Ticket (still true after read)

- **No form validation** to test `empty/garbage` unless chat input counts — confirmed: zero `<form>` except chat.
- **Double-submit = `streaming` gate** — confirmed at `DigitalEricChat.tsx:40` + `216`.
- **External links open `_blank`** — confirmed everywhere except same-page `href="#…"` anchors and `mailto:`.

---

## Sources (files read)

`index.html:1-36`, `src/components/Contact.tsx:1-80`, `src/components/DigitalEricChat.tsx:1-227`, `src/data/caseStudies.ts:57-457`, `src/components/Projects.tsx:1-196`, `src/components/CaseStudyModal.tsx:1-460`, `src/components/Header.tsx:1-65`, `src/components/Hero.tsx:1-189`, `src/components/ResumeModal.tsx:1-133`, `src/components/Credentials.tsx:1-237`, `src/data/credentials.ts:1-275`, `src/components/CustomCursor.tsx:1-55`, `src/hooks/useMousePosition.ts:1-16`, `src/hooks/useTheme.ts:1-88`, `src/hooks/useActiveSection.ts:1-46`, `src/App.tsx:1-39`, `src/index.css:1-111`, `public/` (ls — `resume.pdf`, `profile.*`, `favicon.svg`, `assets/**` — 50+ webp/pdf), `src/assets/profile.png`.

---

## Hand-off to break-it matrix

Tick the matrix against this table row-by-row. Highest leverage first: (1) chat `streaming` + SSE error paths + `maxLength` bypass, (2) `/resume.pdf` 404 + modal scroll-lock stacking, (3) hash routing aliases + back-button, (4) custom cursor on touch + `overflow-x:hidden` at 320px, (5) every external href `target/_blank/rel` + demo 500s. Issue #21 stays open — close after HITL grilling.
