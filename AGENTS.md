# Agent skills

## Issue tracker

Issues live in GitHub Issues on PSergio984/portfolio-website, driven via the `gh` CLI. See `docs/agents/issue-tracker.md`.

## Triage labels

Default five-role triage vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

## Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root, consumed lazily. See `docs/agents/domain.md`.
