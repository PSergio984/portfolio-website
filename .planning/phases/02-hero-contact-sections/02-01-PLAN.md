---
phase: 02-hero-contact-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vitest.config.ts, tests/setup.ts, src/hooks/useTheme.ts, index.html]
autonomous: true
requirements: [CORE-03]
must_haves:
  truths:
    - "Application has a functional testing foundation using Vitest"
    - "Theme management is moved to a custom hook using useSyncExternalStore"
    - "Dark mode preference is applied early in index.html to prevent flash"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Testing configuration"
    - path: "src/hooks/useTheme.ts"
      provides: "State-of-the-art theme management"
  key_links:
    - from: "index.html"
      to: "localStorage"
      via: "blocking script"
---

<objective>
Establish a robust testing foundation and refactor the theme management logic. 

Purpose: Ensure reliability for future refactors and implement the state-of-the-art theme sync recommended in research.
Output: Vitest configuration, useTheme hook, and FOUC (Flash of Unstyled Content) mitigation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-hero-contact-sections/02-RESEARCH.md
@REVIEWS.md
@package.json
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Testing Foundation</name>
  <files>package.json, vitest.config.ts, tests/setup.ts</files>
  <action>
    - Install dev dependencies: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`.
    - Create `vitest.config.ts` extending Vite config to include `test` property with `environment: 'jsdom'` and `setupFiles`.
    - Create `tests/setup.ts` to import `@testing-library/jest-dom`.
    - Add `test` and `test:ui` scripts to `package.json`.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>Testing environment is ready and a simple smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement useTheme Hook</name>
  <files>src/hooks/useTheme.ts</files>
  <action>
    - Create `src/hooks/useTheme.ts` per the pattern in 02-RESEARCH.md.
    - Use `useSyncExternalStore` to synchronize with `localStorage` and `prefers-color-scheme`.
    - Implement `setTheme` and `toggleTheme` methods.
    - Ensure it handles the "system" preference correctly.
  </action>
  <verify>
    <automated>ls src/hooks/useTheme.ts</automated>
  </verify>
  <done>Custom hook created with type-safe theme management logic.</done>
</task>

<task type="auto">
  <name>Task 3: Mitigate Flash of Unstyled Content (FOUC)</name>
  <files>index.html</files>
  <action>
    - Add a small, blocking inline script in the `<head>` of `index.html`.
    - The script should check `localStorage` and `matchMedia` for the dark theme.
    - It must add the `dark` class to `document.documentElement` immediately.
    - Remove redundant background/text classes from `index.html` as recommended in REVIEWS.md.
  </action>
  <verify>
    <automated>grep -q "document.documentElement.classList.add('dark')" index.html</automated>
  </verify>
  <done>Theme is applied before React hydration, preventing white flickers on load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Browser -> localStorage: Theme preference persistence.
- System -> matchMedia: OS-level theme preference.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | localStorage | accept | Low impact; invalid values fall back to 'system' in useTheme. |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for Vitest and RTL packages. |
</threat_model>

<verification>
- `npm test` executes successfully.
- Page load in dark mode does not show white flash.
- `useTheme` exports the expected interface.
</verification>

<success_criteria>
- Vitest/RTL infrastructure is functional.
- Theme logic is centralized and synchronized.
</success_criteria>

<output>
Create `.planning/phases/02-hero-contact-sections/02-01-SUMMARY.md` when done
</output>
