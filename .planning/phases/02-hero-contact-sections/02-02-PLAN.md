---
phase: 02-hero-contact-sections
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/components/Header.tsx, src/components/Hero.tsx, src/components/Contact.tsx, src/components/Footer.tsx, src/App.tsx, src/components/Hero.test.tsx]
autonomous: true
requirements: [CORE-03]
must_haves:
  truths:
    - "App.tsx is refactored into modular components"
    - "New components are integrated and functional"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "Navigation and theme toggle"
    - path: "src/components/Hero.tsx"
      provides: "Intro section skeleton"
    - path: "src/components/Contact.tsx"
      provides: "Contact section skeleton"
    - path: "src/components/Footer.tsx"
      provides: "Page footer"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/Hero.tsx"
      via: "import and usage"
---

<objective>
Refactor the monolithic App.tsx into modular, testable components.

Purpose: Improve maintainability and follow established architectural patterns.
Output: Decoupled Header, Hero, Contact, and Footer components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/App.tsx
@src/hooks/useTheme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Modular Components</name>
  <files>src/components/Header.tsx, src/components/Hero.tsx, src/components/Contact.tsx, src/components/Footer.tsx</files>
  <action>
    - Extract the `<header>`, `<section id="about">` (Hero), `<section id="contact">` (Contact), and `<footer>` sections from `App.tsx` into their own files.
    - Create functional components for each, ensuring Lucide icons are imported correctly.
    - `Header.tsx` should use the `useTheme` hook for the theme toggle.
    - Maintain existing Tailwind classes and skeleton structures (animations) for now.
  </action>
  <verify>
    <automated>ls src/components/*.tsx</automated>
  </verify>
  <done>Component files created with extracted JSX.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor App.tsx and Wire Components</name>
  <files>src/App.tsx</files>
  <action>
    - Remove inlined theme state and effect from `App.tsx`.
    - Import and render `Header`, `Hero`, `Contact`, and `Footer`.
    - Clean up unused imports (useState, useEffect, Lucide icons).
    - Ensure the layout wrapper remains consistent with the professional minimalist aesthetic.
  </action>
  <verify>
    <automated>grep -v "useState" src/App.tsx | grep -q "Header"</automated>
  </verify>
  <done>App.tsx is a clean orchestrator of modular components.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Basic Component Tests</name>
  <files>src/components/Hero.test.tsx</files>
  <action>
    - Create a basic unit test for the `Hero` component.
    - Verify that it renders correctly (even as a skeleton).
    - Ensure the test suite passes using `npm test`.
  </action>
  <verify>
    <automated>npm test src/components/Hero.test.tsx</automated>
  </verify>
  <done>Testing foundation is verified with a real component test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- App -> Components: Prop passing (none yet).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | Components | mitigate | Ensure no sensitive placeholder data is committed. |
</threat_model>

<verification>
- Application renders identically to Phase 1 (visual regression check).
- Theme toggle in `Header.tsx` works.
- Unit tests pass.
</verification>

<success_criteria>
- Clean, modular component architecture.
- Passing unit tests for new components.
</success_criteria>

<output>
Create `.planning/phases/02-hero-contact-sections/02-02-SUMMARY.md` when done
</output>
